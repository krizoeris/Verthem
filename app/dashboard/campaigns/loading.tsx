import React from 'react';
export default function Loading() {
    // You can add any UI inside Loading, including a Skeleton.
    return (
        // replace with loading component 
        <div className="w-[100px] h-[100px] bg-slate-700">test loading</div>
    )
}